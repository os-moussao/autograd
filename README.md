# Autograd
Autograd is an automatic differentiation library (from scratch) that works with Numpy arrays and operations.

It allows you to compute the gradients of an output (loss function) with respect to all its inputs (model parameters), which is essential for training machine learning models using gradient-based optimization algorithms.
